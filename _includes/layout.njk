<!doctype html>
<html lang="en">
<body>
    <header>
        <a href="/">My 11ty Site!</a>
    </header>

    <main>
       {% if title %} <h1>{{title}}</h1> {% endif %}
       {% if not title %} <h1>No title</h1> {% endif %}
        <h2>{{potatos}}</h2>
        {{ content | safe }}
    </main>

</body>
</html>